# 复制为 .env 并修改（或用环境变量启动）
PORT=8787
JWT_SECRET=请改成一段随机长字符串
ADMIN_KEY=请改成你的管理员密钥(发码用)
DB_PATH=./data.db

# ===== 支付宝（电脑网站支付）。不填则站内购买不可用，仍可用兑换码 =====
# 服务器对外可访问地址，支付宝回调/同步跳转用，必须公网可达
# 例：https://api.你的域名  或  http://你的公网IP:8787
PUBLIC_BASE_URL=http://你的公网IP:8787
# 支付宝开放平台应用 APPID
ALIPAY_APP_ID=
# 应用私钥（PKCS8 格式，一行，去掉 -----BEGIN/END----- 头尾与换行）
ALIPAY_PRIVATE_KEY=
# 支付宝公钥（在开放平台「接口加签方式」里查看，一行）
ALIPAY_PUBLIC_KEY=
# 网关，正式环境默认即可；沙箱用 https://openapi-sandbox.dl.alipaydev.com/gateway.do
ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do


# ===== Lemon Squeezy（国际支付 / 个人即可注册 / 全球多货币）。不填则国际支付不可用 =====
# 仅面向海外用户；价格在 Lemon Squeezy 后台按"商品变体"设置，自动按用户所在地货币结算。
# 1) 注册 https://app.lemonsqueezy.com 并创建 Store，建 3 个一次性付费商品(月/季/年)
# 2) Settings → API 生成 API Key；Store 设置里拿 Store ID
# 3) Settings → Webhooks 新建，URL 填 <PUBLIC_BASE_URL>/api/lemonsqueezy/webhook，
#    勾选 order_created 事件，Signing secret 自定义并填到下方 WEBHOOK_SECRET
# 4) 进入每个商品的变体(variant)，URL 里的数字即变体 ID，填到对应套餐
LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_STORE_ID=
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_VARIANT_MONTH=
LEMONSQUEEZY_VARIANT_QUARTER=
LEMONSQUEEZY_VARIANT_YEAR=
